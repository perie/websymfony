{# empty Twig template #}

{% block form_row %}
    <div class="form-horizontal row">
        <div class="col-xs-6">{{ form_label(form)}}</div>
        <div class="">{{ form_widget(form, {'attr': {'class': 'form-control'}})}}</div>
        <p class="alert-danger">{{ form_errors(form)}}</p>
    </div>
    
{% endblock %}